from django.apps import AppConfig
from django.conf import settings
import os
import pickle
import numpy as np



#class ClassificationConfig(AppConfig):
    # default_auto_field = 'django.db.models.BigAutoField'
    # name = 'classification'
    # path = os.path.join('D:/PFEprj/classification/models','models.p')
    # path = os.path.join(settings.MODELS,'models.p')
    # with open(path,'rb') as pickled:
    #     data = pickle.load(pickled)
    # model = data['model']
    # vectorizer = data['vectorizer']
    
    # pathdata = os.path.join(settings.MODELS,'modelsp.p')
    # with open(pathdata,'rb') as p:
    #     data1 = pickle.load(p)
    # y = data1['polarite']
    # x = data1['data']

    # pathdata = os.path.join(settings.MODELS,'modelsasp.p')
    # with open(pathdata,'rb') as p:
    #     data2 = pickle.load(p)
    # aspect = data2['aspect']
    